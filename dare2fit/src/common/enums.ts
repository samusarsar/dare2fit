/* eslint-disable no-unused-vars */
/* eslint-disable no-shadow */

export enum Roles {
    Base = 'base',
    WantAdmin = 'wantAdmin',
    Admin = 'admin',
    Blocked = 'blocked',
}

export enum Units {
    walking = 'steps',
    running = 'km',
    swimming = 'm',
    cycling = 'km',
    workout = 'workouts',
    strength = 'workouts',
    stamina = 'workouts',
    stretching = 'workouts',
}

export enum FriendRequestType {
    sent = 'sent',
    received = 'received',
}
